
@model Pizza

<div class="container">
	<div class="row py-5 m-auto">
		<div class="col-6">
			<h3>@Model.Nome :</h3>
			<h6>@Model.Description</h6>
			<h4>@Model.Price €</h4>
		@*	<h4>@Model.Category.Name</h4>*@
@*			 <p>
                <strong>Ingredienti:</strong>
                @foreach (Ingredient ing in Model.Ingredients)
                {
                    <div>
                        <label>
                            @ing.Name
                        </label>
                    </div>
                }
            </p>*@
		</div>
		<div class="col-6">
			<img width=70% src="@Model.Pic">
		</div>
	</div>
	<div class="text-center">
		<a href="@Url.Action("Index", "pizza")">Torna alla pagina iniziale</a>
	</div>
</div>



